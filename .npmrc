# pnpm configuration
# Allow trusted packages to run postinstall scripts
# This eliminates the "Ignored build scripts" warning during build

# Enable strict peer dependencies
strict-peer-dependencies=false

# Don't hoist these packages (Prisma needs special handling)
public-hoist-pattern[]=*prisma*
public-hoist-pattern[]=@prisma/client
public-hoist-pattern[]=@prisma/engines

# For pnpm 8.x and above, allow these packages to run lifecycle scripts
# Note: On Vercel, scripts are sandboxed for security, so this warning is expected
# The explicit "prisma generate" in our build script handles the Prisma client generation